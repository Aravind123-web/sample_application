apiVersion: v1
kind: deployment
metadata:
  name: web-application
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-application
    template:
      metadata:
        labels:
          app: web-application
      spec:
        containers:
        - name: nginx
          image: nginx
          ports:
          - containerPort: 80
